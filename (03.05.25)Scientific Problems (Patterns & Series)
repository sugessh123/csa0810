# Number series (e.g. 1 to n)
n = 5
print("Number Series:", list(range(1, n+1)))

# Number pattern
for i in range(1, n+1):
    print(" ".join(str(j) for j in range(1, i+1)))

# Pyramid pattern
for i in range(1, n+1):
    print(" " * (n-i) + "*" * (2*i - 1))
